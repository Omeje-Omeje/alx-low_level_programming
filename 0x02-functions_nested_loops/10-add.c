#include "main.h"
#include <stdio.h>

/**
 * add - Function to add two numbers
 * @a: First operand
 * @b: Second operand
 *
 * Return: Always 0 (Success)
 */

int add(int a, int b)
{
	return (a + b);
}
