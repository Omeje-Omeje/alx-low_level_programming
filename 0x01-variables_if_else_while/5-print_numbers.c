#include <stdio.h>
/**
 * main - Function to print 10 numbers
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%i", i);
	}
	putchar('\n');
	return (0);
}
