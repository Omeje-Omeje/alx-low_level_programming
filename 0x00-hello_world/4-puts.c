/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
#include <stdio.h>
int main(void)
{
	char myString[] = "\"Programming is like building a multilingual puzzle";

	puts(myString);

	return (0);
}
