#include "main.h"

/**
 * mul - Function that multiply two numbers
 * @a: First operand
 * @b: Second operand
 *
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	int mul =  (a * b);
	return ( mul);
	return (0);
}
